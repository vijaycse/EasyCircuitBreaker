package com.officedepot.service.generic.client;

import java.util.concurrent.Callable;
import java.util.concurrent.Future;

public interface ODGenericThreadPool {
	Future<?> submitTasks(Callable<?> task);
}
